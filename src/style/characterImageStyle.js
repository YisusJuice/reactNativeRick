import { StyleSheet } from "react-native";

export const characterImageStyle = StyleSheet.create({
    image:{
        aspectRatio: 1.2,
        height: 120,
        borderRadius: 20,
    },

    container:{
        alignItems: 'center',
        margin:10,
    }
})